// Code your solutions in this file
function countDown(startingNumber) {
    // Iterate from the startingNumber down to 0
    for (let i = startingNumber; i >= 0; i--) {
        // Log the current number
        console.log(i);
    }
}

// Example usage:
countDown(10);